/**
 * Seed Script (In-Memory)
 *
 * With the in-memory data layer, seed data is automatically loaded
 * when the server starts. The data files (users.js, doctors.js,
 * patients.js, diagnoses.js) contain pre-populated arrays.
 *
 * Default test credentials:
 *   - admin1 / password123 (Admin)
 *   - dr.sardor / password123 (Clinician)
 *   - receptionist1 / password123 (Receptionist)
 *
 * Note: All data resets when the server restarts.
 */

console.log('In-memory data is automatically seeded when the server starts.');
console.log('');
console.log('Default credentials:');
console.log('  admin1 / password123 (Admin)');
console.log('  dr.sardor / password123 (Clinician)');
console.log('  receptionist1 / password123 (Receptionist)');
